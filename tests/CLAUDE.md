# tests/ — test matrix & guard reference (auto-loaded when working here)

Root CLAUDE.md carries the TDD policy and the docs index (engine knowledge lives in `docs/`); this file is the full matrix of integration/guard scripts and what each pins.

## Testing

**TDD is mandatory for every code change — features, bug fixes, refactors, build-script tweaks, all of it.** No exceptions, no "I'll add the test after the smoke test passes." A live curl against a running server is a useful sanity check but it is NOT a test — it doesn't survive in CI, it doesn't pin the behavior against future regressions, and it doesn't prove the fix is on the right code path.

The order is fixed:

1. **Write the failing test first.** It must fail for the right reason — i.e. the symptom you're fixing or the capability you're adding. If you can't articulate a failing test, you don't yet understand the change you're making.
2. **Make it pass.** Minimum code to flip the test green; no opportunistic refactoring in the same step.
3. **Run the full suite** (`zig build test` + the relevant integration script) — a green new test doesn't excuse a red regression elsewhere.
4. **Then iterate** — refactor / clean up / extend, with the test as a tripwire.

Concrete rules for each change class:

- **Feature**: at least one unit test that fails without the feature and passes with it. Add an integration test (`tests/test_*.sh`) when the feature is observable over HTTP.
- **Bug fix**: a regression test that reproduces the bug from the user's perspective. The diff order must be `test (red) → fix → test (green)` — verify locally that reverting the fix turns the test red again.
- **Cross-arch work**: cover both served architectures (`qwen4_exp`, `mimo_v2`), not just the one you're holding. One arch's pass is not the suite's pass.
- **Refactor / no-behavior-change**: at least one existing test must exercise the refactored path before the change. If coverage isn't there, add a characterization test first, then refactor under it.
- **Untestable surface (build scripts, etc.)**: factor a pure helper out and test that.
- **Class bugs get class guards — no whack-a-mole.** When a live failure reveals a CLASS of bug (an escaping gap, a format-family leak, a dispatch hole — anything where "fix one model" could break or miss another), the fix ships with THREE things: (1) the instance regression test; (2) a corpus entry or universal invariant in `src/format_corpus_test.zig` that exercises the whole class across every family/entry, so future additions are covered automatically; (3) a CLAUDE.md gotcha naming the class, its symptom signature, and the rule that prevents reintroduction. Precedents: the no-tag-leak + valid-JSON-args invariants, the history round-trip serialization invariant, the spec-decode dispatch engagement counts.

Existing tools:

- Unit tests OK; integration tests with real models are the real tests.
- Live scripts and real-pack unit tests find packs at `${SUSHI_MODELS_DIR:-$HOME/.sushi/models}/<pack>` (a per-script
  override variable wins); run outputs land in `~/.sushi/runs/`. No tracked file names a box's own directories.
- After big features: build `sushi` and run the agentic harness.
- Always run `zig build test` before submitting.

| Command | Purpose |
|---|---|
| `zig build test` | Zig unit tests |
| `./tests/test_gpu_lock.sh` | The one-heavy-GPU-job lock (`scripts/gpu-lock.sh`) on a private lock dir: free status, acquire names the holder, a second acquire waits, a non-holder's release is refused, the waiter gets the lock after release, three waiters are served FIFO even when the latest polls fastest, a dead waiter's ticket is skipped, `status` lists holder then queue. No GPU, seconds. |
| `MIMO_STREAM_MODEL=<converted pack> python3 tests/test_mimo_cli.py` | Real-TTY `run` regression: reaches the chat prompt, streams one reply, and exits cleanly on `/bye` within a bounded deadline. Uses a private HOME by default; `MIMO_CLI_TEST_HOME` exercises an existing model-discovery/settings root. `MIMO_SSD_BUDGET_GB` defaults to 100. |
| `MIMO_STREAM_MODEL=<original or converted checkpoint> bash tests/test_mimo_streaming.sh PORT` | Live MiMo MXFP4 streaming: private settings HOME, budget/cache engagement, 80% per-layer cache preload before readiness, text-only model row, deterministic greedy repeats without prefix caching, prefill crossing the SWA window, and named MTP refusal. Original checkpoints also assert native-source loader engagement and required streaming. `MIMO_SSD_BUDGET_GB` selects total residency (default 60 GiB). |
| `SUSHI_MODELS_DIR=<dir> bash tests/test_mimo_ring_reuse.sh` | Live MiMo hot-cache reuse past the sliding ring (kv8, one entry, thinking on): after a reply long enough to compact the ring past the prompt end, an identical re-issue restores from the prompt-end ring checkpoint byte-identically, a turn diverging right after the prompt restores there within 0.5 nats of its cold prefill, and both warm prefills run under 0.7x cold. SKIPs when the reply stays inside the ring. |
| `zig build test -Doptimize=ReleaseFast -Dtest-filter="mimo source"` | Tiny raw-checkpoint loader fixtures: FP8 decoding, rank-local QKV reconstruction, stored-affine trunk linears, EXL3 shard stamps, resident-byte accounting and malformed-input rejection. No full model load. |
| `MIMO_V2_SOURCE=<original HF dir> MIMO_PACK_REFERENCE=<converted pack> zig build test -Doptimize=ReleaseFast -Dtest-filter="real mimo_v2"` | Read-only source configuration/header checks, including equality of prepared resident-trunk bytes with the converted pack. No generation or performance claims. |
| `MIMO_V2_SOURCE=<original HF dir> python3 tests/test_mimo_source_refusals.py` | Real-binary refusals before weight loading: offline resident mode declines the original checkpoint, and serve mode without a streaming budget returns `ExpertStreamingRequired`. No full-model load. |
| `MLX_ENABLE_TF32=0 MIMO_V2_MODEL=<tiny oracle dir> MIMO_V2_FIXTURE=<fixture.safetensors> zig build test -Doptimize=ReleaseFast -Dtest-filter="mimo v2 fixture"` | Independent HF f32 backbone parity: exact embeddings, per-layer streams, full prefill and cached-decode logits. Dense fixture experts are stacked without requantization; TF32 is disabled for the f32 math oracle, not as a production bf16 policy. |
| `MIMO_V2_SOURCE=<HF dir> zig build test -Doptimize=ReleaseFast -Dtest-filter="real mimo_v2"` | Read-only tests of the downloaded Flash configuration and actual Jinja template: layer geometry, sink placement, additive stop set, reasoning round-trip and native XML tool history. Does not load model weights or prove forward parity. |
| conversion — converters, allocators, imatrix drivers, the repacker | MOVED to the private converter repo (its operating notes carry the rows that used to sit here; see the private section of the root CLAUDE.md docs index). This repo keeps the CONSUMER contract: `docs/pack-format.md`, the Zig shard-stamp check, the committed `src/fixtures/`, and `sushi kld`. The oracle dumpers (`tests/dump_*_fixtures.py`) stay — they verify the engine, they do not make packs. |
| `zig build test -Dtest-filter="format corpus"` | Hermetic format-correctness corpus (`src/format_corpus_test.zig`): real captured model outputs across families through splitThinkBlock/stripThinkBlock/parseToolCalls, plus universal no-tag-leak + valid-JSON-args invariants. No weights; harvest workflow in the file header. |
| `./tests/test_smoke_matrix.sh` | Quick hot smoke: both served archs (`qwen4_exp`; `mimo_v2` boots the resident EXL3 pack, adds `--ssd-budget-gb` only when `MIMO_SSD_BUDGET_GB` is set, and skips `mtp`) × server configs (`default`, `kv4`, `kv8`, `mtp` where a head ships, `nospec`) × ~28 API checks per boot: chat stream/non-stream, thinking on (stream and non-stream must agree on the SPLIT) / off, tools, json_schema, logprobs, max_tokens cap, prefix-cache hit, 2-way concurrency, /v1/completions, /v1/messages, /v1/responses, /v1/models, /metrics.json, alive + no `[mlx]` error. Bar per cell is "answers, no leak, no crash" — a checkpoint's choices are branched on, never asserted. Boots each server with an ISOLATED `HOME` (a saved model-settings profile outranks launch flags, so a real profile turns a kv4 cell into whatever the user saved). `SMOKE_ARCHES`/`SMOKE_CONFIGS` (csv), `SMOKE_MAX_GB` (default: no cap), `QWEN4_EXP_MODEL`/`MIMO_STREAM_MODEL` (pack paths), `SMOKE_OUT`. |
| `FORMAT_MODELS=<csv> ./tests/test_format_matrix.sh` | Live format matrix: `qwen4_exp` and `mimo_v2` (resident EXL3 pack) × 8 checks: thinking split, stream+tools content routing, tool-arg byte-fidelity (chat + /v1/messages tool_use), omitted max_tokens. One model at a time on port 11297 with `--log-level debug`; FAILs echo raw-output dumps for corpus harvesting. `QWEN4_EXP_MODEL`/`MIMO_MODEL` override the pack paths; missing paths skip cleanly. |
| `VALIDATOR_MODELS=<csv> ./tests/test_validator_matrix.sh` | API-compliance + agentic matrix: per served architecture (`qwen4_exp`, `mimo_v2`) boots a server, runs llmprobe (`~/projects/agents/llmprobe`, 112 checks across /v1/responses + /v1/chat/completions + /v1/messages), then the pi 2-turn html agentic case (multi-turn tool calls) via pi_integration_run.sh (qwen4_exp only: pi's driver boots without MiMo's streaming budget). `SKIP_PI=1`/`SKIP_PROBE=1` run one layer; pkills ALL serving sushi instances. Logs in tests/validator-results/. |
| `./tests/integration_test.sh [model_dir] [port]` | 36 end-to-end API tests |
| `HOT_SWITCH_MODELS=<csv> ./tests/test_hot_switch_state.sh [root] [port]` | Every model answers with ITS OWN tokenizer, template and grammar table. Several models interleaved on ONE server (A, B, C, then A again — a singleton built by A leaks into B, and A's SECOND visit catches a table rebuilt for B), each round asserting: `/tokenize` returns a stable token count for that model across switches (a foreign tokenizer changes it — the digit-grouping class), a plain turn answers with no markup leak, and `response_format: json_object` yields PARSEABLE JSON. Plus a per-model `[grammar]` build count. The 2026-08-11 process-wide `global_token_bytes` bug is this class and showed ONLY as bad output (`## Attributes` / `郑重(郑重)`) — nothing crashed and nothing logged an error. |
| `LONGCTX_TEST_MODEL=<dir> ./tests/test_tools_long_context.sh [port]` | A tool call still PARSES at 16k/32k/64k, where the sliding-window block trim and spec verify interact. Both are guarded for speed and for byte-identity on prose; neither guarded a tool call at the lengths where the trim actually engages (below ~8k there is nothing to trim, so short tool tests pass for the wrong reason). Per rung: the call fires with the declared name, arguments are valid JSON, a planted identifier survives into them, no markup leaks — and the log is checked for the `[sliding] block trim engaged` line, since a rung that fell back to short-context handling proves nothing. `LONGCTX_RUNGS` overrides. |
| `CONC_TEST_MODEL=<dir> CONC_N=4 ./tests/test_tools_concurrent.sh [port]` | Concurrent agent loops each get THEIR OWN tool calls. Batched decode and tool buffering are each tested alone and never together; a cross-slot leak looks like one client receiving another's arguments, which no single-stream test can see. N streams, each planting a DISTINCT code, assert: every stream reaches `[DONE]`, every call carries its own code and NO other stream's, arguments valid JSON, no markup in content, server functional after. |
| `CANCEL_TEST_MODEL=<dir> ./tests/test_cancel_mid_tick.sh [port]` | Stop one thinking stream every 3 s while a second MTP stream decodes (both `enable_mtp:true`, `reasoning_effort:low`, default the Flash-Next EXL3 pack): the server must survive every cancel and the log must show `[spec-stats] mode=mtp`. A tick whose snapshot still held the cancelled slot read its freed `think_bound` (SIGSEGV in `thinkBoundTick`). Hermetic counterpart: `complete waits out an inference pass that still holds the slot` (scheduler.zig). |
| `CANCEL_TEST_MODEL=<dir> ./tests/test_cancel_mid_tool_buffer.sh [port]` | Disconnect WHILE the stream is buffering for tool detection. That window — server holding an unflushed buffer AND a live slot, emitting nothing until the pattern resolves — is not covered by `test_disconnect_cancel.sh`, which cancels a prose stream where every token has already gone out. Asserts one `[cancel]` per round, no runaway `active_bytes` growth across rounds (the buffer is freed), and that the server still answers with tools and without. |
| `./tests/test_tool_response.sh [port]` | Tool calling round-trip |
| `./tests/test_serving_deps.sh` | The served binary's dependency and refusal contract: `otool -L zig-out/bin/sushi` lists no non-system dylib beyond `libmlxc` and `libwebp`, and still lists `libmlxc`; an unknown flag exits non-zero NAMING itself rather than being eaten by the unknown-arg branch; a checkpoint in an unsupported file format exits non-zero with `unsupported model format` before any load. Static, no model, no server — run it after any `build.zig` link change. |
| `./tests/test_kv_cache_poison.sh [port]` | KV cache poisoning regression |
| `./tests/test_anthropic_api.sh [port]` | Anthropic Messages API |
| `PLD_TEST_MODEL=<dir> ./tests/test_pld_equivalence.sh` | PLD byte-equivalence (default the Flash-Next EXL3 pack) |
| `./tests/test_streaming_pld.sh [port]` | Streaming PLD byte-identical to non-streaming |
| `./tests/test_pld_tools.sh [port]` | PLD with tools present: engages (`[spec-stats] mode=pld`), tool calls + echo text byte-identical to no-PLD baseline (starts its own server) |
| `MTP_TEST_MODEL=<dir> ./tests/test_mtp_equivalence.sh [port]` | Qwen native-MTP head (default the Flash-Next EXL3 pack): auto-load, per-request opt-out, first-100-char temp-0 equivalence vs `--no-mtp` (TIE-AWARE: on a byte mismatch it replays serially with logprobs and acquits only when the first divergent token is a near-tie, top-2 gap ≤ 0.15 nats — verify widths move ties, a plumbing bug diverges at a confident position; servers boot `--prefix-cache-entries 0` per the hybrid byte-stability rule), and a per-request `[spec-stats] mode=mtp` ENGAGEMENT count on chat (stream + non-stream) + /v1/messages — output equality alone can't see a silent fallback to regular decode. Calibrated surfaces additionally set `MTP_EXPECT_AUTO_PROFILE=<enum-tag>` + `MTP_EXPECT_AUTO_DEPTH=<n>` together: the boot log must name that exact full-fingerprint arm, and the max-confidence echo must realize that depth in `[spec-stats]`, so a conservative generic/cap-6 fallback cannot pass merely because MTP still engaged. Includes an ACCEPTANCE-FLOOR check (best avg_per_round >= 0.5 — depth-independent, unlike per_draft_pct which legitimately dilutes to ~25% on chained creative drafts at the depth-3 default): a structurally broken head — e.g. a sidecar built without folding +1 into Qwen's delta-encoded norms — engages, accepts ~0/round, gate-falls-back to regular decode, and passes both equivalence and engagement. MoE targets (default-off MTP) run with `MTP_FORCE_ENABLE=1`, which injects `enable_mtp:true` into every request. Discovery mirrors production: every `mtp.sidecar_rel_paths` filename plus sharded-index and monolithic-checkpoint `mtp_marker_keys` are accepted. Standalone sidecars rebuild from the original Qwen repo with the private converter. |
| `./tests/test_completions_spec.sh [port]` | /v1/completions spec-decode: PLD engages on the FIM endpoint, first-N-token equivalence, stream/non-stream leading-space parity (starts its own server) |
| `./tests/qwen4_ab.sh mtp\|batched <tag> [server flags]` | Same-boot qwen4_exp A/B harness (prompt fixtures in `tests/fixtures/qwen4_ab/`: 400-token code + prose, one 8.4k prompt; nonce varies by REP never by arm): `mtp` = MTP vs serial interleaved x3 per prompt + an MTP-beside-plain concurrent arm; `batched` = serial x3 then 2-/4-stream prose + 2-stream 8.5k aggregates. `SUSHI_BIN` picks the binary (a control binary must sit in `zig-out/bin/` for its rpath), `QWEN4_AB_PORT`, engine env passes through (`SUSHI_MTP_FORCE_DEPTH`, `SUSHI_MTP_TRACE`, kill switches). Numbers in `docs/gotchas/engine-mlx.md`; the M5 box re-measures with the same script. |
| `MTP_FORCE_ENABLE=1 MTP_TEST_MODEL=<qwen4 pack> ./tests/test_mtp_equivalence.sh` | Runs on qwen4_exp (11/11 on the 4-bit pack): the checkpoint marker `language_model.mtp.fc_hidden.weight`, the head's own `[qwen4] MTP head loaded` line, `MTP_FORCE_ENABLE=1` because MoE keeps MTP opt-in per request. Its `--no-mtp` baseline caught the in-checkpoint head ignoring the flag. |
| `MTP_ADAPTIVE_MODEL=<long-ctx MTP pack> ./tests/test_mtp_adaptive.sh [port]` | Adaptive serial switch (speculation turns itself off where a measured plain token beats the planned round). Two boots of one checkpoint, prompts generated in-script (~40k tokens), `SUSHI_ROUND_COST_PERSIST=0`. Pins the invariant, never the model's choice: the switch line or a measured serial cell with the switch on, speculation still engaging, no `-> serial` on a short-request burst, nothing at all with `SUSHI_MTP_ADAPTIVE_SERIAL=0`, and an empty round-cost dir after a `--no-mtp` boot. SKIPs without a candidate pack. |
| `SUSHI_MTP_GROUP_PLANNER=1 SUSHI_MTP_FORCE_DEPTH=4 <native-MTP server booted --log-level debug>` then `python3 tests/test_mtp_batch_stops.py <port> --server-log <that log> [--out <dir>]` | The HTTP-observable half of GROUPED MTP (`enable_batch_mtp` on `/v1/chat/completions`): the caller owns the boot, the checkpoint and the GPU/thermal lease — the script starts nothing, it only talks to the port and reads the log it is handed, and the boot forces depth 4 so the grouped arm and its solo reference share one verify width. Solo references first (`enable_batch_mtp:false`, temp 0): five short list prompts at 160 tokens, a 200-token "survivor" and a 512-token "victim". **Every reference is that prompt's SECOND run** — the first run COMPUTES the prefix and every later run RESTORES it from the prefix cache, and a computed prefix is not bit-exact with its restore on main, so a first-run reference would diverge from the grouped runs it is compared to for a reason that has nothing to do with grouping. Gate [1] **EOS inside an accepted draft block**: each short prompt is fired on a barrier beside the survivor, both grouped, until a log window carries `[mtp-stop] accepted EOS`; the survivor's `[mtp-planner-stats] tokens=200 plain=` must then stay <= 2 (its partner's EOS must not stop the SURVIVING row speculating), and BOTH streams must equal their solo reference event-for-event plus finish reason plus `completion_tokens`. Gate [2] **mid-round cancellation of one row of a group**: the victim is RST mid-stream (`SO_LINGER` 0) after 17+attempt deltas beside the survivor, up to 8 attempts, until `[mtp-publish] cancelled after round` appears; the cancelled stream's deltas must be a PREFIX of its own solo run (a tick that published the gone request's row would diverge here) and the survivor must still equal solo. Both gates additionally require `[mtp-planner-stats]` in their window (two native-MTP streams really entered the group planner), and the run ends on `[batched] row-axis mtp verify engaged (slots=2` — without it every round above was a solo verify and the equalities prove nothing. Pins the INVARIANT, never the model's choice: no text, token count or round shape is asserted, and WHICH iteration reaches an accepted-draft EOS or lands a mid-round cancellation is the model's business — the script fails only when none of them does. Mismatching pairs land as `<label>.expected.json`/`.actual.json` under `--out`. Hermetic counterparts: `qwen4 MTP head: N=1 FORCE_BATCHED draft ids and logits mlx_equal solo at depths 1/2/4` + `MTP head: N=2/4 mlx_equal drafts/logits vs solo` (transformer.zig) and the planner's own unit tests in `src/mtp_group_planner.zig`. |
| `./tests/test_batched_transition.sh [port]` | Concurrent-stream consistency: legacy→batched decode transition must not duplicate/drop tokens (starts its own server). On a GDN trunk (`BATCH_TEST_MODEL=<qwen4 pack>`) this is also the batched-then-serial == all-serial bar: the batched tick advances each slot's `moe_seq_offset` |
| `./tests/test_multiclient_stress.sh [port]` | The "several pi's + several chats" mix on ONE server: concurrent tool-calling agent streams (chat A) + plain streams (chat B, a DIFFERENT model → per-model batch groups), all at once; asserts no lost request, [DONE] termination, server alive + functional after, no crash signature, and log ENGAGEMENT per leg (a silently-skipped leg can't pass on transport). Knobs: `STRESS_THINK`/`STRESS_MTP` (agent leg only), `N_ABORT` (mid-stream client vanish), `N_CHURN` (load/unload cycles), `SOAK_WAVES=N` (repeat against ONE server, /props memory sampled per wave; ≥3 waves asserts final active ≤ 1.5× wave-2 — the leak class), `STRESS_CHAT_A2` (alternate agent model on even waves → registry evict/reload churn under the default 3-model resident cap). Workers RETRY 503s: with >3 models fighting the resident cap and every victim refcounted mid-stream, the load path 503s honestly ("retry after current requests complete") — a lost request is a failure, an honest backpressure 503 is not. |
| `./tests/test_long_agent_memory.sh [port]` | 10-turn Claude-Code-style agent: plants 3 facts in turn 1, recalls them across mode transitions (tools on/off, thinking on/off). Catches "model acts like first-time-seen" regressions. |
| `./tests/test_reasoning_budget_stream.sh [model_dir] [port]` | A tiny `reasoning_budget_tokens` SHORTENS and ENDS the thought on chat (tools, stream + non-stream) and `/v1/responses` (stream + non-stream, `reasoning.effort` xhigh): reasoning streams incrementally, stays inside the budget plus the forced close, the server's early-stop line closes it, and the turn ends with an answer (`stop` / `completed`), never `length`. Starts its own server; default the Flash-Next EXL3 pack. |
| `./tests/test_stream_keepalive.sh [port]` | Streaming-silence class guard: for EVERY streaming surface (chat / messages / responses) a long buffered tool call must never leave the socket silent longer than the keepalive interval — max inter-chunk gap < 15s, at least one `: keepalive`/`event: ping` observed, and the tool call still parses with valid-JSON args (injected keepalive bytes must not corrupt the SSE stream). SKIPs a surface whose generation was too short to fill the buffer. Pre-fix all three FAIL at `max_gap ≈ 17.8s, keepalives=0`. Starts its own server; `KEEPALIVE_TEST_MODEL` overrides the default Flash-Next EXL3 pack. Hermetic counterpart: `StreamHeartbeat` tests in server.zig. |
| `./tests/test_disconnect_cancel.sh [model_dir] [port]` | Client-disconnect handling during long prefills: SSE keepalives flow every 5s while a request waits on first tokens (Anthropic `ping` events / OpenAI `: keepalive` comments), and a vanished client cancels its slot within one probe interval — the chunk loop aborts the ghost prefill (`error.Cancelled`) instead of grinding minutes of abandoned work that piles up behind Claude Code retries. Starts its own server. |
| `./tests/test_cancel_commit.sh [model_dir] [port]` | Cancelled requests PRESERVE their committed KV instead of dying with the slot. Decode-phase: a stream killed mid-generation (the slot bypasses finishSlot via `complete()` → cleanup queue) commits prompt+emitted tokens from the cleanup drain — proven by an identical re-issue reporting `cached_tokens ≈ prompt` plus a `[hot-cache] reused` line. Prefill-phase (hybrid AND plain-attn): a mid-prefill kill commits the FORWARDED PREFIX — the boot pins `--prefill-chunk/--ssm-checkpoint-stride 512` so the cancel window always spans stride boundaries; hybrids ride the Cancelled-checkpoint salvage (`CancelledCheckpointSink` — initWithOptions hands its captured checkpoints + the authoritative `forwarded` count to the slot; `cache.step` reads 0 on every aborted prefill since it only advances at init completion) and the re-issue restores at the last checkpoint ≤ matched. The killed stream's captured body self-diagnoses which phase the kill landed in (content OR reasoning_content deltas ⇒ decode). Warmup request first — headless boots load lazily. Hermetic counterparts: `cancelledPrefillCommitLen` unit test + three source-scan class guards (cleanup-drain commit before deinit; commitSlotIfApplicable routing BEFORE the pad-only guard — `was_pad_only` starts true and a cancelled prefill never emits a token to clear it; sink handoff + `salvage.forwarded` + Slot.deinit salvage free) in scheduler.zig. |
| `./tests/test_messages_stream_thinking_tools.sh [model_dir] [port]` | /v1/messages STREAMING with thinking + tools together (Claude Code's exact shape) on a template-opened-think model: no think-tag leak in text deltas, SSE content-block lifecycle validity (every delta/stop references an open block), thinking_delta + tool_use emission. Starts its own server. Hermetic counterpart: `chat.streamThinkGate` unit tests + the corpus streaming-gate replay. |
| `./tests/test_kv_quant_equivalence.sh [model_dir] [port]` | Off-vs-4-bit-vs-8-bit KV cache equivalence (per-arch first-N-token thresholds, env-overridable). Batched × kv-quant crash guard lives in `test_batched_equivalence.sh` (its last section: `SUSHI_FORCE_BATCHED=1` + `--kv-quant 8` — the batched kernel once fed raw packed KV to SDPA and killed the server on the first concurrent decode). |
| `./tests/test_kv_quant_per_request.sh [model_dir] [port]` | Per-request `kv_quant` body field plumbing — confirms parse + scheduler routing for each scheme. |
| `./tests/test_kv_quant_fused_equivalence.sh [model_dir] [port]` | Packed quantized-KV reads: dense vs fused first-N-token equivalence at `--kv-quant 4` PLUS engagement both ways — the fused arm must log `[kv-attn] fused engaged` AND `[kv-attn] decode kernel engaged`, the dense arm neither (the flag shipped as a silent no-op for months; output equality alone is structurally blind to that). Boots with `SUSHI_KV_ATTN_MIN_TK=1` so the short prompt still exercises the fused paths (the default floor is a perf gate). The spec-on arm (PLD, echo prompt, default model) pins that the DECODE kernel serves only t_q == 1 (`Tq=1` on its one-shot line; verify widths used to fall to the composed packed-qmm chain — the 2.1x decode loss at 8k+, 2026-08-15) plus greedy equivalence vs the dense spec arm — deliberately NO verify-kernel assertion there. The third arm boots a qwen-shaped model (`KV_VERIFY_SPEC_MODEL`, default the Flash-Next EXL3 pack — hd 256, the adoption set) and asserts `[kv-attn] verify kernel engaged` + PLD engagement + equivalence: the dense fallback is output-equivalent, so a verify dispatch hole is invisible to everything but the log. RATE recovery is deliberately not asserted anywhere here (one-run spec cells are variance — it lives in the same-boot bench A/B). Hermetic counterparts: grouped-Q/strided/multi-block parity tests in kv_quant.zig + transformer.zig, `qkvVerParityCase` (TQ 2..8, causal tail mutation-checked), the kernel-or-DENSE source scan, `kvAttnFusedEligible`/`kvAttnVerifyEligible` (red-verified) + `resolveKvAttnFusedPure` unit tests. |
| `./tests/test_prefix_cache_mem.sh [model_dir] [port]` | `--prefix-cache-mem` budget — long-prompt traffic must stay within the byte cap and produce eviction log lines. The #330 arm drives ONE growing conversation past the budget and asserts a `trimmed oversized entry to N/…` line plus a later `reused M/…` with M >= N: from the outside a trim that silently stops engaging is indistinguishable from a legit decline, so only the log pins it (the unit tests own the trim's correctness). A hybrid whose smallest checkpoint outweighs the budget declines legitimately — WARN, gated on the `; ssm` clause in eviction lines; on plain attention a decline IS the cliff and FAILs (red-verified against the pre-fix binary). |
| `./tests/test_port_conflict.sh` | Busy-port refusal: a listener, a bound-but-silent socket, `--host 0.0.0.0`, the `run` path and a second sushi on the first one's port each exit non-zero with `port N is already in use` before any model loads. Hermetic, no GPU, seconds. Unit side: `resolveBind`, `startupRefusal`, `startListener`, `bindAddress` tests. |
| `./tests/test_parent_pid.sh [port]` | `--parent-pid`: headless over an empty `--model-dir` with a `sleep` as the parent. sushi keeps serving while the parent lives, exits 0 with a `[parent-pid] <pid> is gone` line within 10 s of its death, stops a boot whose parent is already gone, and refuses a non-pid value by name. Hermetic, no GPU, seconds. Unit side: `parent_watch` tests. |
| `./tests/test_headless_spec_flags.sh [port]` | Headless (`--serve --model-dir`, no `--model`) must honor the CLI's spec-decode flags — the mode the app ALWAYS launches. `runHeadlessServe` hand-rolled all three PLD fields as `false`/`5`/`3` literals; #95 threaded `--pld` and left `--pld-draft-len`/`--pld-key-len` still dropped. Asserts the boot banner echoes non-default lengths, `--no-pld` still wins, and a bare boot reports 5/3. Section [4/4] pins a second headless-only class: **endpoint existence must not depend on model state** — resolution runs ahead of dispatch, so an unknown path used to answer 503 "No default model configured" instead of 404, and every endpoint-probing client (llmprobe) then read the server as a catch-all and scored all surfaces absent. Checks unknown → 404 (a removed media-generation route included) while served endpoints still report 503. FULLY HERMETIC — an empty `--model-dir` discovers zero models, so no checkpoint is needed and it runs in seconds. Hermetic counterpart: `server.PldDefaults` tests. |
| `LOGPROBS_TEST_MODEL=<dir> ./tests/test_logprobs.sh [port]` | The logprobs wire contract on every surface that serves it. Sections [1]-[3] pin the three defects fixed 2026-08-05 (temperature-scaled logits, float-equality id recovery, a one-token offset) through their ONE observable bar: at temp 0 the emitted token IS the argmax, so rank 1 of `top_logprobs` must equal it — plus the same token's logprob must not MOVE between temp 0 and temp 2 (it is the model's distribution, not the sampler's), the distribution must not be saturated to 0.0, and `/v1/completions` must answer the legacy shape (integer `logprobs`, four parallel arrays, `top_logprobs` a text-keyed map). Sections [4] and [5] are the class those three did not cover: **STREAMING accepted the field, disabled speculation to honor it, computed every entry, and dropped them** — on chat the SSE chunk template had no `logprobs` slot, and on `/v1/completions` the streaming handler hardcoded `logprobs_n = 0` beside a parser that reads it. Each asserts streaming carries them AND agrees with non-streaming token-for-token and VALUE-for-value on the same greedy request, which is what catches a partial drain, a duplicated one, or an off-by-one high-water mark; [5] additionally pins that legacy `text_offset` ASCENDS ACROSS CHUNKS and matches non-streaming, since it indexes the whole completion and a per-chunk reset to 0 is the natural bug. Structurally invisible to everything else: output-equality tests see byte-identical deltas either way, and llmprobe probes logprobs non-streaming only — it scored this server 100% on `Logprob consistency` while streaming returned nothing. Section [6] pins the NEXT layer of the same class: the array was built from the raw `token_ids`, so it described the reasoning block we strip — on every model that thinks, `logprobs.content[0]` was the first token of the THOUGHT (measured 37-186 entries against 8 characters of content). It asserts the entries RECONSTRUCT `message.content` on a thinking request, and — the red-on-revert half — that their count is BELOW `completion_tokens` whenever reasoning was stripped, since pre-fix the two were equal by construction; streaming is held to its OWN deltas, because the gate trims differently and the invariant is that entries describe what was sent. Section [7] pins that every response body is valid UTF-8: a token is a BPE fragment and can hold half a multi-byte character, and raw bytes in a JSON string make the whole body unparseable rather than merely wrong (live: a `b"\xf0\x9f"` candidate). It checks EVERY response the script makes, not one crafted request, because which one draws a split candidate into its top-5 is luck. Any chat model works (default the Flash-Next EXL3 pack); SKIPs without one. Hermetic counterparts: `computeLogprobs` rank/tie test + the `last_logprob` assignment scan (generate.zig), `formatCompletionsLogprobs` shape test, `contentTokenRange`, the UTF-8 lossy-escape test, and the streaming-emitter scan `every streaming chat emitter carries logprobs` (server.zig) — now TWO-SIDED: a content emitter must drain, a reasoning emitter must NOT, so neither "a new emitter forgets" nor "a reasoning chunk ships the thought's entries" can return. |
| `LOOP_TEST_MODEL=<dir> ./tests/test_loop_stop_signal.sh [port]` | The degenerate-tail guard's WIRE contract, on every surface that can emit it. A loop cut reports `finish_reason: "stop"` so clients do not mistake it for output/context exhaustion, and reports its CAUSE beside it as `finish_details:{"type":"repetition_loop"}` — asserted on chat + completions, stream and non-stream. Tool-call parsing is suppressed for that cause, preserving the 2026-07-14 php.html safety invariant without triggering pi's compact-and-retry path. Section [1/6] also pins the TRIM: a non-streaming body must not carry the degenerate tail back, since an agent re-sends the cut turn as history and the model resumes its own loop. Section [6/6] boots with `SUSHI_LOOP_TRIM=0` so the trim's own red-on-revert runs every time. Needs any chat model; SKIPs without one. Hermetic counterparts: `degenerateTail`, `loopStopDecision`, `shouldParseToolCalls`, `finishDetailsField`, and `loopTrimmedIds`. |
| `./tests/test_idle_evict.sh [root] [port]` | `--idle-evict-secs`: a model nobody is using leaves residency and the next request cold-loads it back. Residency is read from the `loaded` rows of `/v1/models`, backed by an RSS arm — "unloaded" with the memory still held is the failure the flag exists to prevent. Three things output alone cannot see. **A status poll must not be a load**: `GET /props` fell through to `ensureLoaded`, so the app's 3 s tray poll (and any status tooling) retook the memory the sweep had just handed back, which from the outside is indistinguishable from eviction being broken. **The reload is where the retained CPU state is freed**, and two readers touch it holding no refcount (`/v1/models` and the pre-load text-gen gate on a chat request), so every cycle fires both concurrently with the reload. **A reload must free the generation it replaces**: RSS is compared between cycles that all end with NOTHING resident, so the only thing that can grow is orphaned CPU state (bar 40 MB over 6 cycles; the leak measured ~196 MB/cycle on Flash-Next). Final arm boots the same binary with no flag and asserts the model STAYS resident — the flag is the gate, not the sweep's existence. 2 s window (the sweep floors at 1 s) so a cycle costs seconds. `IDLE_EVICT_CYCLES`, `IDLE_EVICT_RSS_LIMIT_MB`. Hermetic counterparts: `idleEvictable` + the two `pickIdleEvictable` tests (model_registry.zig), `idleEvictTickMs` (scheduler.zig), `textGenTargetOf` `.loading` predicate (server.zig). The locking discipline itself is comment-pinned — a source scan is the obvious guard and this repo bans them. |
| `./tests/test_route_404_no_load.sh [model_dir] [port]` | A path the server does not serve costs NOTHING. Model resolution ran ahead of dispatch and the existence check lived in `ensureLoaded`'s `NoDefaultModel` arm, so it only fired when there was no model to load: a POST to a bogus path carrying a resolvable `model` cold-loaded that checkpoint and THEN 404'd (found by typing `/v1/load` for `/v1/load-model` — 2m42s and 121 GB resident for a wrong URL, and a one-liner to pin the box). Boots headless over a `--model-dir` with NO `--model` so every entry starts `unloaded`, POSTs three plausible typos/probes naming a real id, and asserts 404 on each AND that the entry is still `unloaded` — the state check is the assertion, the 404s pass either way. Also re-checks that a real route and `/props`-with-no-default still answer 200, since the gate sits above both. Nothing loads when the fix holds; the small model loads when it doesn't, so it is fast either way. Hermetic counterpart: the `answered BEFORE the model is resolved` source-order scan (server.zig). |
| `./tests/test_cold_load_launch_flags.sh [model] [port]` | Launch flags survive a COLD load (hot switch, `/v1/load`, first request naming an unloaded model) — `ensureLoaded` builds a SECOND `LoadRequest` and three rounds of this bug have shipped through it (prefix-cache, MTP, drafter+ssd). Boots the primary, cold-loads a symlink clone carrying an in-dir `drafter/config.json` that declares the DFlash contract and nothing else: the arm that probes fails its load LOUDLY, the arm with `--no-drafter` is silent, and that difference is the assertion — no real sidecar, no GPU, seconds per arm. Baseline arm runs first so a setup that stopped exercising the probe FAILS instead of passing vacuously. Both arms check the `/v1/load-model` HTTP status and the `--no-drafter` arm additionally asserts the clone reached `ready` — "no DFlash line" is also what a cold load that never happened looks like, and a typo'd path 404s while STILL cold-loading the model named in the body, so the silent arm has to prove it did the work. Any small chat model works; SKIPs without one. Hermetic counterparts: the `re-applies EVERY retained launch setting` source scan + `coldLoadDrafterDir` / `coldLoadVision` in scheduler.zig. |
| `./tests/test_json_mode_multi_model.sh [modelA] [modelB] [port]` | The JSON grammar mask is built PER MODEL. One server, two resident models, `json_object` + `json_schema` against A then B then A — under the process-wide `global_token_bytes` singleton (live 2026-08-11) B masked its logits with A's vocabulary, ids meant different bytes there, and the constraint fell open: `## Attributes`, `郑重(郑重)`, `<<<<<<< Vcc`. Which model breaks depends on request ORDER, so a single-model script cannot see it. Also asserts one `[grammar] building token-byte table for <id>` line PER id — a shared table logs exactly one, which is the class assertion output parsing alone would miss on a model that happens to answer JSON anyway. Defaults to the Flash-Next and MiMo EXL3 packs; SKIPs without them. Hermetic counterpart: the `grammar token-byte table is per MODEL` test in model_registry.zig. |
| `./tests/test_json_schema_thinking.sh [model_dir] [port]` | JSON-schema output reaches content (#331): finite-budget Chat/Messages requests keep thinking off, while supported unlimited reasoning defers the grammar until `</think>`. A reasoning-only completion-cap stop preserves empty content, `length`, and exact usage in Chat streaming/non-streaming. Checks schema shape and routing, not model prose. |
| `./tests/test_continuation.sh [model_dir] [port]` | Continuing a partial assistant reply EXTENDS it instead of restarting it. A conversation ending in an assistant message used to render that message as history and then let `add_generation_prompt` open a SECOND assistant turn, and the model answers a doubled turn by starting over. Pins both surfaces asking their own documented way — an explicit `continue_final_message` on `/v1/chat/completions` (vLLM's spelling), an implicit trailing assistant message on `/v1/messages` (Anthropic's own behaviour) — and that the flag's ABSENCE leaves chat-completions unchanged, since agent frameworks legitimately POST assistant-last history there and expect a fresh turn. The "did it restart?" check is `grep -vi` on the prefix, and the without-flag arm is measured on `prompt_tokens` rather than on the reply: "yellow." is the natural next word whether the model is finishing the sentence or answering afresh, so a working feature failed the first cut of that test — what the flag changes is where the prompt ENDS, which costs a close tag plus a turn header and is true of every checkpoint. Greedy throughout (a sampled reply makes a restart check a coin flip). Any small chat model; SKIPs without one. Hermetic counterparts: the five `renderChatTemplate: continuation…` tests (chat.zig — open turn, content channel, whitespace trim, non-assistant decline, muse channel commit), `keyFor: a continuation does not share a key` (tokenize_cache.zig), and `continuationRejectReason` + the `every continuation surface consults` scan (server.zig), which is what keeps an embedded-engine model from reaching a backstop that answers by closing the socket. |
| `./tests/test_api_edges.sh [model_dir] [port]` | Request-validation edges on ONE model (default the Flash-Next EXL3 pack), 58 checks: malformed bodies (empty, invalid, array, no/empty/non-array messages, `n>1`, 3000-deep nesting, wrong method, unknown route, CORS preflight), sampling edges (`top_p 0` == temperature 0, `top_k 1` greedy, out-of-range temperature/top_p/top_k/seed/max_tokens/logit_bias still answer), stop sequences (cut before the match, `""` and `["", …]` ignored, `/v1/messages` echoes the matched `stop_sequence`), structured output (`json_schema` without an object schema is a 400, enum + nested + array schema enforced, `json_object` parses), media on the wire (undecodable/remote `image_url` refused by NAME on chat + messages + responses, in any message), content shapes (text parts join, NUL byte does not truncate, CJK/emoji round-trip, invalid UTF-8 answered, developer role, continuation returns only the tail), embeddings (empty input 400, array answers one vector each), Responses (no input, background, unknown previous id, `text.format` schema enforced), and alive + no MLX error after all of it. Every check is a server CONTRACT, never a checkpoint expectation. Found on first run (2026-09-16): `top_p 0` uniform garbage, `stop ""` empty reply, schema-less `json_schema` falling open, silent image drop, embeddings 500. Hermetic counterparts: `applyTopP at top_p 0 keeps exactly the argmax` (generate.zig), `parseInput records an input_image the decoder could not read` (responses.zig), `ollama: generate with no prompt is the load handshake` (ollama.zig). |
| `BATCH_FEAT_MODEL=<qwen4_exp pack> ./tests/test_batched_features.sh [port]` | Heterogeneous requests in ONE batched decode group (default the Flash-Next EXL3 pack, `--max-concurrent 8 --prefix-cache-entries 0`): seven requests fire at once — streamed tools with a planted code, `json_schema`, thinking, logprobs, a stop sequence, and two plain requests capped at 5 and 300 tokens — and each must keep ITS OWN contract (the call carries its own code with valid-JSON args, the schema conforms, reasoning and content both arrive with no tag leak, logprobs rank 1 == the chosen token and reconstruct content, the stop cuts before `seven`, each cap finishes by its own `max_tokens`). Asserts `[batched] … engaged (slots=N)` with N >= 2 in the log — grammar, logprobs and spec-active slots go serial BY DESIGN (`[batched] slot serial: <reason>`), so the group is the two plain-capped rows plus the stop/thinking rows; without the engagement line the run proves nothing. Alive + no MLX error after. |
| `./tests/test_prefill_interleave.sh [port]` | Prefill-side interleaving: a cold prefill must not stall concurrent decode streams for its whole duration. Two arms of the SAME binary (`SUSHI_PREFILL_INTERLEAVE=0` vs default-on) each run a stream + an ~8k-token cold prefill at `--prefill-chunk 1024`; asserts the on arm logs `[interleave] engaged` and the off arm doesn't (the silent-no-op class), the on arm's max inter-token gap is under HALF the off arm's (measured: 428ms → 114ms), and the stream's greedy output hash matches across arms (interleaving reorders ticks, never math). Needs a small chat model (`INTERLEAVE_TEST_MODEL`); SKIPs without one. Hermetic counterparts: the chunk-loop yield scan (generate.zig) + the hook-wiring/prefill_ns-billing scan (scheduler.zig). |
| `./tests/test_vision_chunked_prefill.sh [port]` | Vision prefill chunks like text (issue #197): the whole-prompt single forward made the memory guard bill full width, so long conversations + one screenshot 400'd unfixably. Two arms of the SAME binary (`SUSHI_VISION_CHUNKED=0` vs default-on) at `SUSHI_PREFILL_CHUNK=32` with a prompt padded past `TAIL_MERGE_MAX` so boundaries land INSIDE the image's placeholder span. Asserts the on arm logs `[vision] chunked prefill` and the off arm doesn't (silent-no-op class), and that BOTH arms perceive the SAME color set on a four-quadrant image — the class bug (splice rows restarting per chunk) collapses the bottom quadrants onto the top colors. Deliberately does NOT assert byte-equality across arms: chunk width changes GEMM shapes and 4-bit near-ties flip across widths. Checkpoint-agnostic color bar. Defaults to the Flash-Next EXL3 pack (Qwen3-VL tower); SKIPs without a vision model. Hermetic counterparts: `spliceVisionRows` chunk-equivalence + audio-order + video-rows tests (transformer.zig — the video one is the guard for the 2026-08-26 hole where Qwen `<|video_pad|>` rows were encoded, inserted and never scattered), `countSpliceRows` + wiring scan (generate.zig), guard-billing scan (server.zig). |
| `./tests/test_connection_thread_reaping.sh [port]` | Per-request pthread stacks are reclaimed. Boots headless, records macOS `vmmap` Stack-region count, sends 300 fresh HTTP connections, then asserts the count did not grow with the request count and the server remains healthy. Reproduces a long-running workload's 4–5 GB → 10 GB process-footprint climb where `/props cache_bytes` was only 2.9 MB: pre-fix each discarded joinable `std.Thread` handle retained one stack mapping plus page-table cost. Hermetic counterpart: `each connection thread handle is detached after spawn` in server.zig. |
| `./tests/test_mlx_error_recovery.sh [model_dir] [port]` | An MLX error costs one request, never the server (#353). `SUSHI_MLX_FAULT_CHUNK=<n>` / `_STEP=<n>` latch a synthetic Metal OOM at the n-th prefill-chunk / decode-step checkpoint and disarm: a **503** with a body for the faulting request (streaming: the mapped SSE `error` event), `[mlx]` in the log, the process alive, and **200 with content** on the next request; plus a long prompt with no `max_tokens` (the omitted-sentinel class). |
| `./tests/test_sleep_inhibit.sh [model_dir] [port]` | Checks no assertion while idle, assertion during generation and startup load, release after work, and `--no-prevent-sleep`. Needs macOS and a small chat model; otherwise skips. |
| `./tests/test_model_rescan.sh [port]` | `POST /v1/models/rescan`: discovery walks the `--model-dir` roots ONCE at boot, so a model the app's Model Browser downloads mid-session was invisible to `/v1/models` (and to the media panes' "On This Mac" rows, which read it) until a restart. Boots headless on an empty root, drops a fake `minimax_h3` dir in after boot, and asserts rescan reports `{"added":1}`, the stub appears in `/v1/models` advertising its media capability, and a second rescan adds nothing (add-only, idempotent — existing entries are never re-pointed or removed). FULLY HERMETIC — stubs never load weights, so a config.json + stub safetensors is enough. Hermetic counterpart: `ModelRegistry: rescan absorbs newly downloaded dirs` (model_registry.zig). |
| `./tests/test_load_model_default.sh [port]` | `/v1/load-model` `"default": true` re-points the server's default model — the app's picker hot-switches a running server instead of restarting it, and a hot-load alone leaves the default untouched, so requests that omit `model` (the "sushi" alias: the Claude Code launcher env) and `/v1/models`' default-first sort (what the app's pill/tray read back) stayed on the OLD model. Three arms: a side-load WITHOUT the flag leaves the default alone (media-gen loads must never steal the chat default); `default:true` promotes; an omitted-`model` chat request routes to the new default (told apart by the echoed `model_type`). NEEDS REAL MODELS — only a load that SUCCEEDS may be promoted, so stub dirs can't reach the branch; skips when `BOOT_MODEL`/`SWITCH_MODEL` (Flash-Next + MiMo EXL3 packs by default) are absent. App counterparts: `AppStateUseModelTests` (`modelSwitchAction` — hot-switch by ABSOLUTE PATH, restart only mid-boot), `APIClientLoadModelTests` (`loadModelBody` — the flag rides switches only). |
| `./tests/test_launch_cmd.sh [model_dir] [port]` | `sushi launch <agent>` (issue #188): unknown agent errors naming the choices; dead server + `--no-start` instructs `sushi serve` (never a silent hang); against a live server, `--print` scripts for omp/codex/claude export the right dedicated-dir env vars (`PI_CODING_AGENT_DIR` for omp — the OMP_ rename never reached the env read) and the written configs carry the server's ADVERTISED context, never a hardcoded one (omp `contextWindow` plus its per-model `thinking` block, codex `model_context_window` + `wire_api = "responses"`, claude's `CLAUDE_CODE_MAX_CONTEXT_TOKENS` verbatim + derived `CLAUDE_CODE_MAX_OUTPUT_TOKENS`); extras after `--` ride the invocation line shell-quoted. pi's `models.json` maps every thinking level (`thinkingLevelMap`, no `thinkingFormat`) to a word in the row's `reasoning_efforts`, and each mapped word answers 200. Needs a chat model (default the Flash-Next EXL3 pack); SKIPs without one. Hermetic counterparts: builder/budget/arg-parse tests in `launch.zig`. |
| `./tests/test_metrics.sh [model_dir] [port]` | Observability (`--metrics`): 503-when-off on `/metrics` + `/metrics.json`; Prometheus text shape (`vllm:`/`sushi:` names, TTFT histogram +Inf bucket, gauges); counters/histograms increment after one chat; live-gauge invariants (`generation_tokens_live > 0` after a request, `== generation_tokens_total` at rest); `/metrics.json` JSON shape. No admin/auth surface (retired). Hermetic counterparts: `metrics.zig` unit tests (incl. `liveGenerationTokens`), `scheduler.zig` `sumInflightGeneratedTokens`, `status.zig` `getAppMemFootprintMb`, `server.zig` `apiKeyAuthorized`/`ipIsLoopback`. |
| `./tests/test_prefix_cache_disk.sh [model_dir] [port]` | SSD prefix-cache persistence: restart round-trip (persist → kill → reboot → `[disk-cache] restored` + byte-identical output + ≥2x TTFT win), chunked on-disk layout, multi-turn append, `--prefix-cache-disk off`, and a **hybrid section** (Flash-Next GatedDeltaNet — persist SSM checkpoints → restart → `restored … (ssm@pos)` + byte-identical). Isolated HOME — never touches the real kv-cache. Hermetic counterparts: DiskTier round-trip/extend/GC/salvage/flush-cap + hybrid SSM round-trip/retention/salvage/flush-cap + v4 spec-snapshot round-trip (MTP sidecar, v3-restores-clean) tests in `kv_disk_cache.zig`, two-tier restore/invalidation + hybrid two-tier restore/RAM-beats-disk + spec-snapshots-survive-restart tests in `prefix_cache.zig`. |
| `SOAK_DURATION_HOURS=N ./tests/test_soak_24h.sh [model_dir]` | Plan 01 A8 — 4-way concurrent soak (chat/agent/Anthropic/tool). RSS drift bounded; default 24h, set `SOAK_DURATION_HOURS=1` for CI smoke. |
| `./tests/bench.sh [--only <substr>] [--url <host:port> -m <id>] [--full]` | THE bench. Boots sushi (boot → probe → kill → settle, one model at a time) while **llmprobe** takes every number — `--bench-only`, `--full` for median-of-3 per rung to 64k. Each cell is sushi at its FASTEST: `--mtp` is forced wherever the checkpoint ships a head (default-OFF on MoE targets is exactly where it pays), everything else is already default-on. The engaged mode is read off the server's own `[spec-stats] mode=` lines and printed beside the number, so a mode that silently stops engaging shows up as a bare cell. Another engine = start it yourself and pass `--url host:port -m <id>`: same script, same probe, and nothing about their binaries/ports/versions lives here (the LM Studio daemon that must never be killed is why). Output is paste-ready markdown rows for `benchmarks.md`; reports + server logs land in `~/.sushi/runs/bench-<tag>/`. No CSVs, no charts. |
| `tests/fwd_ubench.sh <model-dir> [iters] [flags...]` | Diagnostic, NOT a test. Boots one model, fires `SUSHI_DECODE_FWD_UBENCH` at load and prints the ms/forward + CPU-build/GPU-eval split + ops/forward + terminal lm_head ablation, then shuts down. The unit of every decode-perf A/B in this repo: run it once per boot per arm and ALTERNATE the arms per boot (a block-of-N schedule reads drift as an effect). Companion env probes: `SUSHI_LAYER_CAP=N` (slope vs fixed cost) and `SUSHI_DISPATCH_PROBE=N` (prices one GPU dispatch by injecting N exact-1.0 multiplies per MoE layer — pure and consumed, so MLX can't elide it and nothing is mutated). |
| `tests/greedy_ab.sh <model-dir> <ENV_VAR> <off-value> [max_tokens] [prompt]` | The acceptance bar for any change that claims to preserve output: boots the same model twice, once per arm of a kill switch, and diffs the temp-0 chat continuation. Reports `completion_tokens` and `finish_reason` so a short/early-stopped generation can't pass as agreement — a 53-byte completion proves nothing. Use ≥ 200 tokens; the numerics classes this catches (INT4 near-tie argmax, a 1-in-65536 activation rounding) surface between tokens 50 and 100, not at token 5. |
| `tests/prefill_ab.sh <model-dir> <ENV_VAR> <on> <off> [reps]` | Prefill-time A/B for a kill switch, reading the SERVER's `timings.prompt_ms` rather than client wall clock. Boots on/off/on/off so a drifting machine cannot land on one arm. Each request carries a nonce so the prefix cache cannot serve the body, and a reading whose `cached_tokens` exceeds the chat-template header (~40) is FLAGGED — a cached prefill is not a prefill. Used to retire mlxfast item 1: the fused-QKV prefill hypothesis measured null, with the two blocks contradicting each other. |
| `QWEN4_MODEL=<pack> ./tests/test_qwen4_exp.sh [port]` | Qwen3.8-Flash-Next live end-to-end on the converted pack (booted `--max-concurrent 4 --prefix-cache-entries 0`; 38 checks): [7b] the image turn under `enable_mtp` (head reads the slot's M-RoPE table: one more `[spec-stats] mode=mtp` + tie-aware == the serial image answer; the tie helper compares lead-STRIPPED text — the first logprob token is `" House"` while `content` is lead-trimmed, which read as a 2-nat divergence at token 0), [11] a SECOND boot `--no-vision` (tower load line absent, `capabilities` drop `vision`, `active_bytes` lower than the post-image sample by >= 400 MB, text answers, the image turn is a 400 naming the tower — it was a 200 with a hallucinated answer). Then: [8] two concurrent 400-token plain requests → `[batched] gdn batched decode engaged (slots=2)` + each answer == its own serial answer (30 tokens, tie-aware ≤0.15 nats), [9] the same with two ~3k prompts (batched decode under the per-slot QSA mask, both needles), [10] an `enable_mtp` request beside a plain one (MTP slot exclusive, plain interleaves; exactly one more `[spec-stats] mode=mtp`, both == serial). `BATCHED_TEST_MODEL=<pack> ./tests/test_batched_equivalence.sh` runs on this arch too (forced N=1 byte-identical + two-stream arm; measured 4/4 on the 4-bit pack). Hermetic twin: `qwen4 batched decode: one forwardMoeBatchedDecode tick == two serial ticks` (tiny fixture, cos 1.00000, per-slot PLE history / QSA key rows / pooled blocks equal). Then: architecture + n-gram-table boot line, greedy short answer, tool call + args, streaming cleanliness, a prompt past the 2048-token QSA budget asserted through `[qsa] sparse attention engaged` + `[qsa-fused] engaged` (the mask arm of `sushi_attn_p256`) + a needle, [5b] the same prompt under MTP (booted `SUSHI_MTP_FORCE_DEPTH=3`) asserting the needle + `[sdpa-split] masked arm engaged` (verify rows 3..15 under an array mask were MLX's unfused fallback), and the MTP head (`enable_mtp:true` on this MoE target): `[spec-stats] mode=mtp` engagement, greedy MTP == serial, acceptance floor 0.5/round; [7] an image turn on `house.jpeg` asserting 200 + a relevant word + the `Vision encoder: Qwen3-VL ViT` load line + the `M-RoPE: 1 images` engagement line (SKIPs when the pack has no `model-vision.safetensors`). SKIPs without the pack. Hermetic counterparts in transformer.zig: `gdn packed prework: bit-identical to the composed chain at decode and prefill widths …`, `gdn norm-gate fused: bit-identical to rms_norm + silu(z) * y …`, `gdn gate: compiled closure vs graph chain vs prework kernel (bit-identity probe)` (pins the assumption the fused GDN decode rests on), `ssm checkpoint carries the qwen4_exp aux state … through capture and restore` (prefix cache on qwen4), plus the extended `DiskTier: hybrid entry round-trips` in kv_disk_cache.zig (`l{d}.aux/pooled/ple` + `qsa_ratio`). Decode meter: `SUSHI_DECODE_FWD_UBENCH=N` (+ `_S=<rows>` verify width, `_KV=<tokens>` prefill first). |
| `QWEN4_MODEL=<pack> ./tests/test_qwen4_mtp_head_persist.sh [port]` | The qwen4_exp in-checkpoint MTP head's committed history rides the prefix cache. Two boots, one long needle prompt sent twice: persist ON asserts `[hot-cache] reused` + `[qwen4] MTP head restored`, persist OFF (`SUSHI_MTP_HEAD_PERSIST=0`) asserts no restore line, and both answer the needle the same way. SKIPs without the pack. |
| `./tests/test_vision_prefix_cache.sh [model] [port]` | Image conversations reuse the prefix cache (vision slots were excluded from commit AND lookup, so every image turn re-prefilled the conversation). Every message's media is placed at its template placeholder: an image followed by user-role context still lands in its own message, a text continuation after an assistant boundary re-places the historical image and restores it from the cache (`cached_tokens > 0`), twenty historical images all decode under one M-RoPE table, and `continue_final_message` keeps its image. Same image twice → `cached_tokens > 0` + `[hot-cache] reused`, warm == cold bytes, the answer still names PIXEL-ONLY content (the street signs' "Grey Fox" / "Waterfall"); a DIFFERENT image under the same placeholder tokens → `cached_tokens 0` (`vision_key`); a new image-bearing turn on the first image restores its image prefix and reads the stop sign. Defaults to the Flash-Next EXL3 pack; SKIPs without a vision model. No byte-equality across warm/cold (hybrid restore class: one token moved on qwen4). Measured on qwen4_exp 2026-08-26: 483/514 reused, foreign image 0, follow-up 483/508. Hermetic counterparts: the `vision_key` arms of `HotPrefixCache: findBestMatch` + `an entry holding the request's first images restores up to the new one` (prefix_cache.zig), the `readOpenAiMessages` / `readAnthropicMessages` / `placeMedia` / `mediaChain` tests (server.zig), `parseInput places input_image parts` (responses.zig), the wire-shape media corpus entry (format_corpus_test.zig) + `vision prefill checkpoints SSM state only when it chunks like text` (generate.zig — the first live run was a `hybrid miss` because vision prompts never checkpointed). |
| `tests/dump_qwen4_exp_fixtures.py build\|dump` + `QWEN4_TEST_MODEL=<pack> QWEN4_FIXTURE=<file> zig build test -Dtest-filter="qwen4 fixture"` | The qwen4_exp engine oracle on a TINY random model (torch venv with transformers main): `build` writes a random checkpoint in the real naming, the converter packs it, `dump` runs the HF reference on OUR dequantized weights (input ids, full-prefill logits, prefill+stepwise-decode logits past the QSA budget, per-layer streams, layer-0/1/3 sub-module captures, the reference n-gram row ids and QSA mask). The Zig test prints a per-layer bisect ladder and asserts cos > 0.995 + argmax agreement on full prefill AND on chunked prefill + 6 decode steps, the MTP head against a torch rendering of the vLLM/SGLang math (synthetic `mtp.*` in the tiny checkpoint), verify→rollback(accept 1)→continue == fresh forward (GDN + PLE + QSA state), and the MTP head's CROSS-ROUND bookkeeping (prefill history → two rounds of chain draft → capturing verify → accept 1 then 0 → trunk rollback → Phase 5a stash → consume-time `qwen4MtpTruncate` → the third round's merged head forward == a fresh head over every committed row, cos 1.00000; head KV + QSA aux keys + pooled blocks past the tiny budget of 8). `build --vision` / `dump --vision` add a tiny tower (depth 2, hidden 64) and dump ONE image prompt (`pixel_values` + grid, the tower's `pooler_output`, 3-D `position_ids` + `rope_delta`, full/prefill/decode logits, streams, the layer-3 QSA mask); the converter packs it with `--add-vision --src`; `QWEN4_FIXTURE_VISION=<file>` runs `qwen4 fixture vision`: tower rows vs the reference, `getRopeIndex` == the reference table, full prefill cos > 0.995 + argmax, the layer-3 QSA mask EXACT (pooled block keys at 3-D block-start angles; scalar-roping them red-probes at 32 mismatches), a two-chunk prefill split inside the image span (bar = our own full forward) + 6 decode steps past the table. Both dumps run every expert (`num_experts_per_tok = num_experts`) and emit the reference's own per-row `qsa_gap` / `logit_margin` / `route_gap` (+ `mtp_route_gap`); `Qwen4Ties` acquits rows under 3% (5% for a routing margin). `build --yarn` writes YaRN `rope_parameters` (window 8 -> 32, beta 1.0/0.1 so the ramp still splits the 4 frequency pairs) into the tiny config; the converter copies it through, `dump` reads it back into the reference, and the SAME test then measures the context extension end to end at positions past the window (`~/.sushi/runs/qwen4-tiny-yarn/`; the test asserts `yarnActive()` == the config; the YaRN fixture against the no-YaRN pack — identical weights — fails at MTP head cos 0.971). `zig build test` CACHES a passing env-gated run (env vars are not in its cache key): rerun the test binary from `.zig-cache/o/<hash>/test` directly to re-execute. `build --topk 2` makes a k < E pack (`~/.sushi/runs/qwen4-tiny-k2/`) whose trunk ties in 18/20 rows across 8 MoE layers — its SELECTION coverage is the MTP head's one MoE layer (12/19 decisive, cos 1.0000); run it with the same env vars, not in CI — a random tiny MoE ties everywhere and our kernels flip one pick per prompt at any router scale or seed (story in models-media.md). Hermetic twin: `mrope fillCosSin strided rows == every stride-th row` in mrope.zig. `qwen4 deferred PLE` (same `QWEN4_TEST_MODEL`, no fixture): two `ple_defer` decode steps + `flushDeferredPle` == the direct forward bit for bit, an unflushed leaf differs. SKIPs without the env vars. Acceptance meter: `SUSHI_MTP_FORCE_DEPTH=n` (every round drafts n, controllers never demote) + `acc_idx=a0/a1/…` on the `[mtp-trace]` line (hermetic: `MtpTrace per-index acceptance` in generate.zig); out-of-tree harness `acc.sh <tag> <pack> <depth>`. |
| `tests/dump_mimo_v2_fixtures.py OUT_DIR [--seed N --ref PATH --offline]` + `python3 tests/test_dump_mimo_v2_fixtures.py` | The `mimo_v2` (MiMo-V2.6) engine oracle on a TINY random-weight `MiMoV2Config` built from the HF reference (pair downloaded/cached under `~/.cache/sushi/mimo-v2-ref`; `--ref`/`MIMO_V2_REF` supplies a local pair; `--offline` is hermetic): `config.json` + `model.safetensors` in HF checkpoint naming + `fixture.safetensors` (per-layer streams/attn/mlp outs, softmax-with-sink probs incl. the dropped sink column and `vis_from`, MoE scores + topk ids + weights pre/post `norm_topk_prob` + rank margins, GA+SWA rope cos/sin, FULL prefill AND cached-decode passes with the observed prefill-vs-cache margins recorded in the metadata). The script self-verifies on reload (`--verify` reruns the battery; exit 2 = loud SKIP when torch is missing, never faked fixtures). Guard `tests/test_dump_mimo_v2_fixtures.py` pins the fixture schema for the Zig test, byte-determinism per seed, and that self-verify goes red on a corrupted fixture; silent on pass, `MIMO_V2_FIXTURE_DEBUG=1` for detail. |
| `tests/dump_mimo_v2_mtp_fixtures.py OUT_DIR` (after `dump_mimo_v2_fixtures.py OUT_DIR`, same seed) + `MLX_ENABLE_TF32=0 MIMO_V2_MODEL=OUT_DIR MIMO_V2_MTP_FIXTURE=OUT_DIR/mtp_fixture.safetensors zig build test -Doptimize=ReleaseFast -Dtest-filter="mimo mtp heads"` | MiMo's three MTP heads on the tiny oracle model: three random heads rendered from the HF reference's own modules (SWA attention with sinks, SwiGLU, norms) composed as vLLM/SGLang's MiMo-V2 MTP layer, head k fed the trunk's final-normed hidden at p and token p+k+1. The Zig test replays prefill history in uneven chunks past the window, then draft rounds with accepted and WRONG drafts and the next round's rollback, and holds every head's output (2e-4) and head 0's logits (2e-3) to the oracle. `MIMO_V2_MODEL` alone also runs `mimo v2 verify rows equal serial decode ticks` (2- and 4-row verify forwards == serial decode logits bit for bit, dense and kv8, across the window edge). |
| `python3 tests/dump_qwen4_yarn_fixtures.py` + `QWEN4_YARN_FIXTURES=<file> zig build test -Dtest-filter="qwen4 yarn parity"` | HOST SPECTRUM oracle for YaRN context extension (inv_freq + per-position cos/sin vs HF `ROPE_INIT_FUNCTIONS["yarn"]` cross-checked against vLLM's transcription). Not the QSA indexer oracle — that stays `dump_qwen4_exp_fixtures.py`. Default `--attn_factor 1.0` so HF replace and vLLM multiply agree; HF `attention_factor` replaces, vLLM `attn_factor` multiplies. |
| `./tests/test_no_local_paths.sh` | Repo-hygiene gate: fails with `file:line` when a tracked file names a development box's own directories (a real home, its scratch dirs); release.yml runs it and `test_release_workflow_gates.sh` pins that step. Silent when clean. HERMETIC |
| `./tests/test_release_workflow_gates.sh` | Static guard for `.github/workflows/release.yml` event gating: PR trigger present, fork-PR job guard, Create Release gated OFF for `pull_request`, tag creation dispatch-only, the pre-release checkbox minting its suffix where the tag is minted while the release stays a draft, `Notarize CLI` NOT excluded on PRs, exactly one `-Dgit-sha` release build, the NAX guard unconditional, artifact upload covering PRs, the version step reading build.zig.zon through release.sh (SemVer: a `26.9.x` CHANGELOG heading, a two-part version and a tag other than `v<zon>`/`v<zon>-pre-release.<n>` are refused; nothing comes from the date), `LICENSE-APACHE-2.0` + `NOTICE` packaged into the CLI tarball, `LICENSE`, `LICENSE-APACHE-2.0`, `NOTICE` present at the root, every repo path `NOTICE` names existing, and `guest.json` (from `--guest-manifest`, version-checked) in the tarball with the tarball's `.sha256` published beside it. Pins the class "a PR build accidentally releases" / "PR artifacts silently stop being notarized". Hermetic — PyYAML parse, no runners. |
| `./tests/test_mlx_staged_nax.sh` | Static guard for the self-built mlx + mlx-c stage (`scripts/build-mlx.sh` from the pinned submodules): staged dylibs + metallib + `.version` stamp exist, the metallib actually contains `*_nax` kernels (MLX's 26.2 CMake gate fails SILENTLY — the brew bottle ships NAX-off), `libmlx.dylib` minos ≥ 26.2, `libmlxc.dylib` links the staged mlx (no `/opt/homebrew` refs), and a built `zig-out/bin/sushi` links no brew mlx/mlx-c. Hermetic — no GPU, no server. |
| `./tests/test_qwen_video_input.sh [model] [port]` | Qwen video input (4 distinct frames as one `video_url`): ≥ 2 of the frames' subjects named AND something only the PIXELS supply — frame 3's sign text "STOP" / "Grey Fox" / "Waterfall" (the generic-subject bar passed for weeks on a build that encoded the frames and never spliced them). On a checkpoint that cannot read the signs this is a checkpoint expectation, not a wire failure (qwen4_exp reads them). |
| `./tests/test_multi_model_dir.sh [port]` | `--model-dir` is REPEATABLE and every folder it names is served. HERMETIC — the "models" are empty dirs holding a config.json, so this pins DISCOVERY and the arg loop, which is where the bug was; nothing is ever loaded. The flag took ONE directory, so a library spread over more than one folder (the app's download destination, the folder it used to be, an LM Studio tree) had the rest listed by the app's own picker and ABSENT from `/v1/models` — switching to one of those cost a full server restart instead of a hot swap, because the id was not in the registry. Four arms: three folders all discovered; a duplicate id resolving to the FIRST folder (the two copies declare different `model_type`s, so `meta.architecture` names which one won — a count of one would pass either way) plus its skip logged; a single folder byte-unchanged; an unreachable folder SKIPPED with a warning, not fatal (the second folder can live on an external drive); and a 9th `--model-dir` REFUSED by name rather than silently dropped (the silent-flag-eater class). Hermetic counterparts: `discoverModelsMany` (model_discovery.zig), `ModelRootsTests` (swift — root ordering, de-dup, the server-cap bound, and a source scan that no site builds `--model-dir` or the models root itself). |
